# Vaani Shopping Assistant - Serverless & Cloud Lightweight Dependencies
# Note: Heavy PyTorch & Whisper packages (~5GB) are excluded here to fit within Cloud/Serverless size limits (500MB).
# For full local PyTorch GPU/CPU Whisper execution, install requirements-full.txt instead.

fastapi>=0.104.0
uvicorn>=0.23.2
python-multipart>=0.0.6
pydantic>=2.0.0
scikit-learn>=1.3.0
